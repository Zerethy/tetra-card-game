{
  "name": "aetherbound",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Aetherbound — a parchment card duel inspired by classic grid battles",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/game.test.js"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
